package ru.regenix.jphp.tokenizer.token.stmt;

import ru.regenix.jphp.tokenizer.token.TokenMeta;

public class OrStmtToken extends LogicStmtToken {
    public OrStmtToken(TokenMeta meta) {
        super(meta);
    }
}
