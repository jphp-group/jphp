package ru.regenix.jphp.tokenizer.token.expr.operator;

import ru.regenix.jphp.tokenizer.TokenMeta;

public class ArrayGetUnsetExprToken extends ArrayGetExprToken {
    public ArrayGetUnsetExprToken(TokenMeta meta) {
        super(meta);
    }
}
