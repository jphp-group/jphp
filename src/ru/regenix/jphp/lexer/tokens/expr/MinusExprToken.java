package ru.regenix.jphp.lexer.tokens.expr;

import ru.regenix.jphp.lexer.tokens.TokenMeta;

public class MinusExprToken extends ExprToken {
    public MinusExprToken(TokenMeta meta) {
        super(meta);
    }
}
