package ru.regenix.jphp.lexer.tokens.expr;

import ru.regenix.jphp.lexer.tokens.TokenMeta;

public class EqualExprToken extends ExprToken {
    protected EqualExprToken(TokenMeta meta) {
        super(meta);
    }
}
