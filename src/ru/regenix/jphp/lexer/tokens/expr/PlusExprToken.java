package ru.regenix.jphp.lexer.tokens.expr;

import ru.regenix.jphp.lexer.tokens.TokenMeta;

public class PlusExprToken extends ExprToken {
    public PlusExprToken(TokenMeta meta) {
        super(meta);
    }
}
