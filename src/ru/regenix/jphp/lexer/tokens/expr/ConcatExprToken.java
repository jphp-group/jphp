package ru.regenix.jphp.lexer.tokens.expr;

import ru.regenix.jphp.lexer.tokens.TokenMeta;

public class ConcatExprToken extends ExprToken {
    protected ConcatExprToken(TokenMeta meta) {
        super(meta);
    }
}
