package ru.regenix.jphp.tokenizer.token.expr.value;

import ru.regenix.jphp.tokenizer.TokenMeta;

public class PrintNameToken extends NameToken {
    public PrintNameToken(TokenMeta meta) {
        super(meta);
    }
}
